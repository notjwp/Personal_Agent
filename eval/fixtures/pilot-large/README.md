# catalog

A product catalogue toolkit: pricing, ranking, filtering, formatting and
a large collection of small helpers.

## Layout

`catalog/` holds one module per concern. See the module docstrings.

## Tests

    pytest -q
