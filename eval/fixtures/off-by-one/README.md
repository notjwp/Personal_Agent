# windows

Sliding-window helpers over sequences, plus rolling statistics built on them.

## Layout

- `windows/sliding.py` — `rolling_window`, `chunk`, `pairwise`
- `windows/stats.py` — `moving_average`, `moving_max`
- `windows/validate.py` — shared argument checks

## Tests

    pytest -q
