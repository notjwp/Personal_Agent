# settings

A tiny settings store with typed lookups and defaults.

## Layout

- `settings/store.py` - the key/value store
- `settings/loader.py` - lookups with fallbacks
- `settings/validate.py` - value rules

## Tests

    pytest -q
